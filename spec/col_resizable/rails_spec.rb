require "spec_helper"

RSpec.describe ColResizable::Rails do
  it "has a version number" do
    expect(ColResizable::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
